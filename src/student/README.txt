1. Arish Soomar
2. No notes.
3. For contains and getSequence since they rely on binary search algorithm
I am claiming O(log n).
4. The huffman decode method is O(b) beacause each bit in the binary sequence, algorithm 
performs a constant-time operation.